## CI (GitHub Actions) — later

This repository intentionally does **not** enable CI for the initial OSS release.

### Enable CI later

1. Create `.github/workflows/` if missing.
2. Copy `docs/ci-workflow.template.yml` to `.github/workflows/ci.yml`.

